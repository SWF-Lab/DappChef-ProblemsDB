// SPDX-License-Identifier: MIT
pragma solidity ^0.8.17;

contract answer33 {
    error ZeroAddress();
    function assemblyCheckZeroAddr(address toCheck) public pure returns (bool success) {
        assembly {
            if iszero(toCheck) {
                // TODO: you should finish this function check out if the address `toCheck` is a zero address or not. 
            }
        }
        return true;
    }
}